## MicroLean

A minimalistic variant of Lean written in Zig. For learning purposes.

Done:
- Type-checking kernel supporting a critical subset of Lean's `expr` kinds: `bvar`, `sort`, `const`, `app`, `lambda`, `forallE`

ToDo:
- inductive types
- finish level parameters
- add `letE`
- user-facing names, pretty-printing
- more caching
- heartbeats etc
- lexer, parser layers


### AI policy

100% human-generated.

